# budonotebook.click — redirect page

Static one-page site deployed via GitHub Pages.

Informs visitors that the old Django website is closed and redirects them to the Budo Notebook Obsidian vault template on GitHub.

## Deploy

GitHub Pages → source: `main` branch, root `/`.

The live URL maps to `budonotebook.click` via a custom domain CNAME.
